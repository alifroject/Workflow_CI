name: Workflow_CI_Project
docker_env:
  image: continuumio/miniconda3
entry_points:
  main:
    parameters:
      test_size: {type: float, default: 0.2}
    command: "python modelling.py --test_size {test_size}"